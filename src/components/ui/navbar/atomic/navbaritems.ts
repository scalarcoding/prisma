import { NavbarItemType } from "./navbarmenutype";

// Define the array with the type
export const navbaritems: NavbarItemType[] = [
    { label: "Home", path: "/" },
    { label: "Category", path: "" },

];
